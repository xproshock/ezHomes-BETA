# ezHomes
> A customizable Spigot Minecraft plugin for setting, removing, and going to your homes with a nice GUI

To compile run `./gradlew build` in your terminal

**This is _only_ tested in Windows**
